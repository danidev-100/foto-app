# OpenSpec Configuration — foto-app
# Generated by sdd-init on 2026-05-19
# Mode: hybrid (openspec files + Engram)

context:
  project: foto-app
  stack:
    language: Go 1.26.3
    framework: Fiber
    database: PostgreSQL via pgx
  architecture: Clean/Hexagonal (target)
  description: >
    Photo application scaffold. Go + Fiber backend with PostgreSQL
    persistence. New project — no code yet.

strict_tdd: false
strict_tdd_reason: >
  No test runner detected. The project is in initial scaffolding phase.
  Strict TDD (test-first) is not enforceable until a test runner is configured.
  Enable once `go test` is wired into the workflow (e.g., after first `go mod init`
  and `go test ./...` passes).

testing:
  runner:
    detected: false
    reason: "Go module not initialized yet — no go.mod, no test files, no test runner configured"
  layers:
    unit: { available: false, tool: "— (go test after go mod init)" }
    integration: { available: false, tool: "— (httptest or testcontainers-go)" }
    e2e: { available: false, tool: "— (playwright or chromedp)" }
  coverage:
    available: false
    command: "go test -cover ./... (after go mod init)"
  quality:
    linter: { available: false, command: "golangci-lint run (not installed)" }
    type_checker: { available: true, command: "go build ./... (built-in)" }
    formatter: { available: true, command: "go fmt ./... (built-in)" }

phases:
  propose: { rule: "require human intent and scope" }
  spec: { rule: "write delta spec with requirements and scenarios" }
  design: { rule: "create technical design and architecture" }
  tasks: { rule: "break into implementation tasks" }
  apply: { rule: "implement tasks per spec and design" }
  verify: { rule: "execute tests and prove spec conformance" }
  archive: { rule: "sync delta specs upon completion" }

persistence:
  mode: hybrid
  engram: true
  openspec_dir: openspec/
